package com.cognate.stream;

public class IntermediateOperationExample {

	
	//Intermediate Operation:
	
	//The operation which performed on stream and 
	//those return stream back are called Intermediate Operation
	//Like distinct(), filter(), map() etc
	
	
	
	
	//Terminal Operation:  return something other than stream
	//Like forEach(), collect(), reduce()
}
